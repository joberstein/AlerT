<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.jesseoberstein.alert">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">

        <activity android:name=".activities.view.Routes">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <meta-data
                android:name="android.app.default_searchable"
                android:value=".activities.RoutesSearchActivity" />
        </activity>

        <activity
            android:name=".activities.search.Routes"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            android:windowSoftInputMode="stateVisible"
            android:parentActivityName="com.jesseoberstein.alert.activities.view.Routes">

            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>
            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable"/>
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.jesseoberstein.alert.activities.view.Routes"/>
        </activity>

        <activity
            android:name=".activities.view.Alarms"
            android:parentActivityName="com.jesseoberstein.alert.activities.view.Routes">

            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
            </intent-filter>
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.jesseoberstein.alert.activities.view.Routes"/>
        </activity>
    </application>

</manifest>